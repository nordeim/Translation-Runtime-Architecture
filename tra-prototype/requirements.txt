# Runtime dependencies (mirror of pyproject.toml [project.dependencies]).
# Install via: pip install -e ".[dev]"  (preferred) or pip install -r requirements.txt
pydantic>=2.8
markdown-it-py>=3.0
diskcache>=5.6
pyyaml>=6.0
click>=8.1
rich>=13.7

# Dev / tooling (install via: pip install -e ".[dev]")
# pytest>=8.2
# ruff>=0.5
# mypy>=1.10
